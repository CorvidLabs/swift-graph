schema_version = 1
profile = "standard"
[lifecycle]
command = ["fledge", "lanes", "run", "verify"]
[contract]
enabled = true
require_coverage = 0
skip_reason = "No prior SpecSync coverage threshold exists; existing cross-platform Swift builds and tests remain blocking"
[risk]
threshold = "block"
[provenance]
mode = "soft"
policy = ".attest.json"
skip_reason = ""
[atlas]
enabled = false
skip_reason = "Standalone DocC Pages publication remains independently managed"
